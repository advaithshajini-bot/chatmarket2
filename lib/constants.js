// Platform commission rate, matching the 18% example in docs/razorpay-integration.md.
// This is charged nowhere for real yet — no real payment integration exists — but
// earnings figures are computed as if it were, so the numbers a seller sees here
// match what they'd actually get once Razorpay Route is wired up.
export const PLATFORM_FEE_PCT = 0.18;
